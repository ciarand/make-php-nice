<?php

/**
 * A collection of (arguably) useful helpers
 */

/**
 * The "…" method functions similarly to Perl's "..." operator (the "yada, 
 * yada, yada" operator) and dies if it's ever called. It's used as a stub for
 * unwritten code.
 *
 * @inspiration Perl6
 */
function …()
{
    die("Method stub");
}
